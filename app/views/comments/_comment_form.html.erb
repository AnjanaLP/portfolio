<%= form_for @comment do |f| %>
  <div class="form-group">
    <%= f.text_area :content, class: "form-control", rows: 5, placeholder: "Add your comment" %>
  </div>

  <div class="form-group">
    <%= f.submit "Post comment", class: "btn btn-primary" %>
  </div>
<% end %>
